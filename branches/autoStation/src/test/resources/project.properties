# Properties for tests. overrides the same file from main folder
database.driverClassName=com.mysql.jdbc.Driver
database.url=jdbc:mysql://localhost:3306/schema
database.username=
database.password=l